# Legacy Components

This directory documents archived/frozen flow parts.

Current legacy flow (not moved yet, but officially archived):
- `main.py`
- `src/*`
- `config/faceit_config.py`
- `config/opencv_config.py`

Policy:
- Keep runnable for backward compatibility where required.
- Do not add new product features here.
- Any new pipeline work must go to `services/*` and `apps/*`.
