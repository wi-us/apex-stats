# Web App (Next.js)

UI for:

1. Tournament selection
2. Match selection
3. Map selection
4. Team filters
5. Timeline slider
6. Map track rendering

## Run

```bash
npm install
npm run dev -w @apex/web
```

Set API URL:

```bash
NEXT_PUBLIC_API_URL=http://localhost:4000
```
