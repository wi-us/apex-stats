# Установить отдельно от scripts/tracking/requirements.txt — пакеты тяжёлые.
#
# CPU:
#   pip install -r requirements-ml.txt
#
# CUDA 12.1 (рекомендуется, если есть NVIDIA GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements-ml.txt
torch>=2.2
torchvision>=0.17
Pillow>=10
scikit-learn>=1.3
tqdm>=4.66