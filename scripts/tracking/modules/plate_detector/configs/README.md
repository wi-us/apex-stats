# Configs Moved

Runtime configuration is centralized under:

```text
scripts/tracking/configs/
scripts/tracking/configs/plate_detector/
```

This module keeps code and local assets only. Pass explicit config paths through
`run.ps1` when experimenting, or use the defaults from the tracking wrapper.
